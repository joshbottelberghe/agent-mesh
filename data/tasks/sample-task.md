---
id: sample-task
title: Sample task so a fresh clone has something to list
status: active
priority: medium
---

This file exists only so `list_tasks` returns something on a fresh clone.
Point `tasks_dir` at your own task directory in config.yaml.
